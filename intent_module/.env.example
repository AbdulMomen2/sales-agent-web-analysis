# Redis
REDIS_URL=redis://localhost:6379

# Server
PORT=3000
HOST=0.0.0.0

# Session TTL (seconds)
SESSION_TTL_S=1800

# WebSocket max payload (bytes)
WS_MAX_PAYLOAD=262144

# Pipeline tick interval (ms)
TICK_INTERVAL_MS=1000
